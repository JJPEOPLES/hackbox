import React from 'react';
import VncViewer from '../gui/VncViewer';
import Layout from '../layout/Layout';

const GuiAccess = () => {
  return (
    <Layout>
      <VncViewer />
    </Layout>
  );
};

export default GuiAccess;